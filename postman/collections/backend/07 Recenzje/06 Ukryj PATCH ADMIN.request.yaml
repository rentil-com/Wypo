$kind: http-request
name: Ukryj recenzję (PATCH, admin)
url: "{{base_url}}/recenzje/ukryj/{{review_id}}"
method: PATCH
headers:
  - key: Authorization
    value: "Bearer {{api_key}}"
    disabled: true
order: 6000


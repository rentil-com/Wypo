$kind: http-request
name: Odkryj recenzję (PUT, admin)
url: "{{base_url}}/recenzje/odkryj/{{review_id}}"
method: PUT
headers:
  - key: Authorization
    value: "Bearer {{api_key}}"
    enabled: false
order: 9000


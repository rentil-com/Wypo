$kind: http-request
name: Edytuj konto (PUT)
url: "{{base_url}}/account/edit/{{account_id}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "imie": "Jan",
      "nazwisko": "Nowak",
      "email": "{{new_email}}",
      "password": "{{new_user_password}}",
      "rola": "uzytkownik"
    }
order: 8000


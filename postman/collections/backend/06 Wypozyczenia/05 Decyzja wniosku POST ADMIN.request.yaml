$kind: http-request
name: Decyzja wniosku (POST, admin)
url: "{{base_url}}/wypozyczenia/wnioski/{{rental_id}}"
method: POST
headers:
  - key: Content-Type
    value: application/json
    disabled: false
  - key: Authorization
    value: "Bearer {{api_key}}"
    disabled: true
body:
  type: json
  content: |-
    {
      "status": "odrzucony"
    }
order: 5000


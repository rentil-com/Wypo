$kind: http-request
name: Lista promocji (ADMIN)
url: "{{base_url}}/promocje?strona={{page}}&stan=aktywna"
method: GET
headers:
  - key: Authorization
    value: "Bearer {{api_key}}"
    disabled: true
queryParams:
  strona: "{{page}}"
  stan: aktywna
order: 2000


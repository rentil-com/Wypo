$kind: http-request
name: Pobierz ustawienia workera (ADMIN)
url: "{{base_url}}/worker/settings"
method: GET
headers:
  - key: Authorization
    value: "Bearer {{api_key}}"
    disabled: true
order: 1000


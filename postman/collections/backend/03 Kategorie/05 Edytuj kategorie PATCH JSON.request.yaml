$kind: http-request
name: Edytuj kategorię (PATCH, JSON)
url: "{{base_url}}/kategorie/edit/{{category_id}}"
method: PATCH
headers:
  - key: Content-Type
    value: application/json
    disabled: false
  - key: Authorization
    value: "Bearer {{api_key}}"
    disabled: true
body:
  type: json
  content: |-
    {
      "nazwa": "Nowa nazwa kategorii",
      "zdjecie_url": "https://example.com/nowa-kategoria.jpg"
    }
order: 5000


services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        EXPO_PUBLIC_API_URL: ${EXPO_PUBLIC_API_URL}
    ports:
      - "8081:80"
    restart: unless-stopped
